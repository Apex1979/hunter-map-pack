# Hunter

Map-pack command center: Run Hunter → Build / Steal / Protect → Approve / Reject.

**Grok Build:** read `GROK.md`, then paste `GROK_PROMPT.txt`.

```bash
npm install
npm run dev
```

Open http://localhost:43127 and click **Run Hunter**.
